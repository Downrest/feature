[package]
name = "feature"
description = 'Personal CI tool for creating "features" in my Roblox architecture.'
license = "MIT"
version = "1.0.3"
edition = "2024"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
toml = "0.9"
